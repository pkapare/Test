Run `pnpm lint` across the workspace. Group errors by rule. For the top 3 most frequent violations, explain the rule and show a before/after fix example. Auto-fix what you can with `pnpm lint --fix`.
