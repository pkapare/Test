Review the current changes for:

1. Security concerns — secrets in code, injection risks, unvalidated input
2. Missing test coverage for new public methods
3. Hardcoded infrastructure (URLs, connection strings, file paths)
4. Descriptive commit message readiness

Use `git diff --staged` (or `git diff` if nothing staged) to see the changes.
Provide a structured review with severity levels: CRITICAL, WARNING, INFO.
